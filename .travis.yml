language: python
python:
  - "3.6"
  - "nightly"
install:
  - pip install -r requirements.txt
before_script:
  - cd postprocess/py
script:
  - pytest --cov=./ -v tests/
after_success:
  - codecov
notifications:
  email:
    recipients:
      - ankursinha@fedoraproject.org
    on_success: never
    on_failure: always
