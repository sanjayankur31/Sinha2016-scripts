language: python
python:
  - "3.6"
  - "nightly"
install:
  - pip install -r requirements.txt
script:
  - cd postprocess/py/ && pytest --cov=./ -v tests/
after_success:
  - cd postprocess/py/ && codecov
notifications:
  email:
    recipients:
      - ankursinha@fedoraproject.org
    on_success: never
    on_failure: always
